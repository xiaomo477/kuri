# 2026-08-25

- 修复 kuri 主题侧边栏 AI 工具图标不显示的问题：根本原因是主题内置的 Material Icons 字体只有 57KB，是 MDUI 精简子集，`smart_toy` / `auto_awesome` 都不在子集内。将其替换为 Google Fonts 完整版 woff/woff2（172KB/128KB）后，`smart_toy` 可正常渲染。
- 已恢复 `_config.kuri.yml` 中 AI 工具图标为 `smart_toy`。
- 新增侧边栏 CSS 美化：一级分类 hover 图标放大/旋转 + 左侧色条；二级菜单子项依次滑入 + hover 位移/高亮；支持 prefers-reduced-motion。
- 原精简字体已备份至 `.workbuddy/backups/MaterialIcons-Regular.woff2.subset-backup`。
- **hexo server 不会自动重建 CSS**：修改 `themes/kuri/source/css/style.css` 后，必须 `hexo clean && hexo generate` 或手动重启 server，否则 `public/css/style.css` 仍为旧版本，用户看到的页面无变化。教训：任何 theme CSS 修改后都要主动 `hexo clean && hexo generate`，并确认 `public/` 的文件 mtime 是新的。
- 修复侧边栏折叠菜单项之间的"大透明空白"：根本原因是 `.blog-sidebar .mdui-collapse-item-body { padding: 4px 0 8px !important; }` 让未展开的 body 也占 12px 高度，多个 body 累积约 300px 的空白。改为：折叠时 `padding: 0`，`.mdui-collapse-item-open > .mdui-collapse-item-body` 时恢复原 padding。Sidebar 高度从 976px 降到 578px。
- 配套美化：profile 区头像加外发光 + hover 微旋转；社交图标改成圆形按钮 + hover 蓝色光晕；分隔线改成水平渐变淡出；collapse-item 间距统一 4px + 圆角 10px。
